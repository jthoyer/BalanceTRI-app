# Balance Tri Club — Race commitment prototype

Open `index.html` in any modern browser. The prototype supports member selection, race filtering, commitments, custom event distances, new races, and browser-local persistence.

Use **Reset demo** to restore the seeded data.

Saving a commitment or adding a race requires a shared club PIN — ask a club
committee member for the current PIN if you're prompted for one.
